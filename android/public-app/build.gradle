plugins { id 'com.android.application' }

android { namespace 'com.khabardhun.publicapp'; compileSdk 35
    defaultConfig {
        buildConfigField "String", "BACKEND_URL", "\"" + (project.findProperty("BACKEND_URL") ?: "https://YOUR-RAILWAY-DOMAIN") + "\""
        applicationId 'com.khabardhun.app'; minSdk 23; targetSdk 35; versionCode 1; versionName '1.0.0' }
}
