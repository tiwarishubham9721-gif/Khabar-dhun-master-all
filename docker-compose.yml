services:
  khabar-dhun:
    build: .
    ports:
      - "8000:8000"
    environment:
      OWNER_USERNAME: owner
      OWNER_PASSWORD: CHANGE_ME
      JWT_SECRET: CHANGE_ME_LONG_RANDOM
      COOKIE_SECURE: "0"
    volumes:
      - kd_data:/data
volumes:
  kd_data:
